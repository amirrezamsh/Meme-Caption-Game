// imports
import express from "express";

// init express
const app = new express();
const port = 3001;

// activate the server
app.listen(port, () => {
  console.log(`-----Please run index.js instead-----`);
});
